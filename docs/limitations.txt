1. Wild guess, kemungkinan model hanya bisa mengenali font size sesuai yang telah di training, jadi jika menggunakan training data dengan font 16, maka model akan menggambar bounding box dengan size 16 juga (jika lebih akan dicrop). Hal tersebut berpengaruh pada font size estimator. Atau bisa juga karena dataset mengandung cursor yang cropped (tolong cek).
2. cursor detection accuracy turun drastis ketika window vscode di scroll horizontal, penyebab belum diketahui.
3. OCR masih memiliki akurasi yang sedikit rendah, gimana kalo training neural network khusus?
4. Tensorflow dan POHMM menggunakan dependency yang tidak compatible (tf requires numpy < 1.19, pohmm requires otherwise), harus dicari solusinya. Solusi sementara: write semua ke pickle, load setelah selesai dengan dependency yang berbeda.

Backup Plan:
Kalo misalkan emang hampir mustahil, backup plan (last resort) nya adalah buat character recognition dengan template matching, jadi scope nya terbatas pada VSCode aja dulu, baru nanti dikembangin lagi.